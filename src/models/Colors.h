#pragma once

#include <SFML/Graphics/Color.hpp>
#include "Colors.h"

const sf::Color MENU = sf::Color(240, 240, 240);
const sf::Color BUTTON = sf::Color(220, 220, 220);
const sf::Color PRESSED_BUTTON = sf::Color(255, 190, 143);
